#! /bin/bash
if [ ! -e config.ini ]; then
    echo "Copying sample ini to config.ini"
    cp config.sample.ini config.ini
fi
XX=simplepush
GOPATH="$(pwd)/Godeps/_workspace:$(pwd):$GOPATH" godep go build -o $XX
GOPATH="$(pwd)/Godeps/_workspace:$(pwd):$GOPATH" ./$XX $*
